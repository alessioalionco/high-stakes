__pycache__/
*.pyc
.env
.high-stakes.toml
high-stakes-runs/
*.egg-info/
dist/
build/
# the engine's DEFAULT write target (`Path.cwd()/outputs`): cost ledger,
# catalog snapshot, cells. Running the tool from the repo root would generate it
# here — and without this line, the repo would commit its own execution state.
outputs/
